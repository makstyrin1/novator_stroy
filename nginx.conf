upstream building {
    server web:8000;
}

server {
    listen 80;
    server_name novator-stroy.ru www.novator-stroy.ru;

    location / {
        proxy_pass http://building;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /app/staticfiles/;
    }

    location /media/ {
        alias /app/media/;
    }
}

# HTTPS (пока закомментирован, потом добавим с Let's Encrypt)
# server {
#     listen 443 ssl;
#     server_name novator-stroy.ru www.novator-stroy.ru;
# 
#     ssl_certificate /etc/letsencrypt/live/novator-stroy.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/novator-stroy.ru/privkey.pem;
# 
#     location / {
#         proxy_pass http://building;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# 
#     location /static/ {
#         alias /app/staticfiles/;
#     }
# 
#     location /media/ {
#         alias /app/media/;
#     }
# }